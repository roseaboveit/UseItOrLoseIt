<%= form_with(model: recipe_note) do |form| %>
  <% if recipe_note.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe_note.errors.count, "error") %> prohibited this recipe_note from being saved:</h2>

      <ul>
        <% recipe_note.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
